import React from "react";
import { db } from "../lib/firebase";
import '../styles/globals.css'

type Props = {};

const HomeScreen = (props: Props) => {
  return (
    <div>
      <h1 className="text-white">adhfaidf</h1>
    </div>
  );
};

export default HomeScreen;
